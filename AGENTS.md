# 项目开发约定

## 业务列表

所有新增或修改的业务数据列表（表格、卡片、弹窗、记录）必须提供分页。使用 `PagedTable`、`PagedList` 或 `ListPagination`，默认每页20，可选10/20/50/100；仅结果不超过200条时允许“全部”。服务端也必须校验全部上限。导航、固定字段选项和图表数据不属于业务列表。

已有集合接口使用显式 `listPage=true` 的兼容分页契约；新建和高频大数据接口优先用数据库 COUNT + LIMIT/OFFSET，返回 `total/page/size/items`，授权与筛选条件必须一致。分页、搜索和排序在服务端完成；禁止把硬截断结果标为完整总数。更新 `docs/LIST_PAGINATION_INVENTORY.md` 并通过分页守卫测试。

## 网关

站点、分组、模型分别管理，不用来源类型代替站点身份。同步走持久化任务和统一分组锁，日志不含凭据。变更售价先预览，应用时核对版本；保留手工售价覆盖。不要提交私有配置、凭据或运行日志。
